Algoritmo "circulo"

Var

   raioCirculo, area : real

Inicio

   escreva("Digite o valor do raio do circulo: ")
   leia(raioCirculo)
   
   area <- Pi * Exp(raioCirculo, 2)

   escreva("Área do circulo: ", area:2:3)

Fimalgoritmo